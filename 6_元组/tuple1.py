# 定义元组
triangle = (100, 200, 300)

# 访问元组数据
print(triangle[0])
print(triangle[1])
print(triangle[2])

# 遍历元组
for n in triangle:
    print(n)

# 定义只有一个元素的元组
nums = (1,)

# 元组元素不可修改
# triangle[0] = 1000
